const icons = {
  dashboard:'<rect x="3" y="3" width="7" height="7" rx="2"/><rect x="14" y="3" width="7" height="7" rx="2"/><rect x="3" y="14" width="7" height="7" rx="2"/><rect x="14" y="14" width="7" height="7" rx="2"/>',
  inbound:'<path d="M12 3v12m0 0 4-4m-4 4-4-4"/><path d="M5 19h14"/>', outbound:'<path d="M12 21V9m0 0 4 4m-4-4-4 4"/><path d="M5 5h14"/>',
  boxes:'<path d="m12 2 4.5 2.5L12 7 7.5 4.5 12 2Z"/><path d="m16.5 4.5 4 2.3-4.5 2.6L12 7"/><path d="m7.5 4.5-4 2.3L8 9.4 12 7"/><path d="M8 9.4v5.2l4 2.3 4-2.3V9.4"/><path d="m8 14.6-4.5 2.6 4.5 2.6 4-2.3m4-2.9 4.5 2.6-4.5 2.6-4-2.3"/>',
  database:'<ellipse cx="12" cy="5" rx="8" ry="3"/><path d="M4 5v6c0 1.7 3.6 3 8 3s8-1.3 8-3V5"/><path d="M4 11v6c0 1.7 3.6 3 8 3s8-1.3 8-3v-6"/>',
  network:'<circle cx="12" cy="5" r="2.5"/><circle cx="5" cy="18" r="2.5"/><circle cx="19" cy="18" r="2.5"/><path d="m10.8 7.2-4.5 8.4m6.9-8.4 4.5 8.4M7.5 18h9"/>', sparkles:'<path d="m12 3 1.3 3.7L17 8l-3.7 1.3L12 13l-1.3-3.7L7 8l3.7-1.3L12 3Z"/><path d="m19 14 .8 2.2L22 17l-2.2.8L19 20l-.8-2.2L16 17l2.2-.8L19 14ZM5 13l.8 2.2L8 16l-2.2.8L5 19l-.8-2.2L2 16l2.2-.8L5 13Z"/>', brain:'<path d="M9.5 4.5A3 3 0 0 0 4 6v.5A3.5 3.5 0 0 0 4.5 13 3 3 0 0 0 9 17.5V20m5.5-15.5A3 3 0 0 1 20 6v.5a3.5 3.5 0 0 1-.5 6.5 3 3 0 0 1-4.5 4.5V20M9.5 4.5A3 3 0 0 1 12 6a3 3 0 0 1 2.5-1.5M9 10a3 3 0 0 0 3-3v13m3-10a3 3 0 0 1-3-3"/>', wifi:'<path d="M5 12.5a10 10 0 0 1 14 0M8.5 16a5 5 0 0 1 7 0M12 20h.01M2 9a15 15 0 0 1 20 0"/>', target:'<circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><circle cx="12" cy="12" r="1"/>', arrowRight:'<path d="M5 12h14m-5-5 5 5-5 5"/>',
  user:'<circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 3.6-7 8-7s8 3 8 7"/>', lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>', eye:'<path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6S2 12 2 12Z"/><circle cx="12" cy="12" r="2.5"/>',
  login:'<path d="M10 17l5-5-5-5"/><path d="M15 12H3"/><path d="M14 3h5a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-5"/>', logout:'<path d="m14 8 4 4-4 4"/><path d="M18 12H7"/><path d="M11 4H5a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h6"/>',
  shield:'<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z"/><path d="m9 12 2 2 4-4"/>', bell:'<path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M10 21h4"/>',
  package:'<path d="m21 8-9-5-9 5 9 5 9-5Z"/><path d="m3 8 9 5 9-5v9l-9 5-9-5V8Z"/><path d="M12 13v9"/>', alert:'<path d="M10.3 3.5 2.2 18a2 2 0 0 0 1.8 3h16a2 2 0 0 0 1.8-3L13.7 3.5a2 2 0 0 0-3.4 0Z"/><path d="M12 9v4m0 4h.01"/>',
  hospital:'<path d="M3 21h18M5 21V5h14v16M9 8h6M12 5v6M8 14h2m4 0h2m-8 3h2m4 0h2"/>', activity:'<path d="M3 12h4l2-7 4 14 2-7h6"/>',
  clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>', search:'<circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/>', plus:'<path d="M12 5v14M5 12h14"/>', download:'<path d="M12 3v12m0 0 4-4m-4 4-4-4M4 21h16"/>',
  chevron:'<path d="m9 18 6-6-6-6"/>', check:'<path d="m5 12 4 4L19 6"/>', trash:'<path d="M4 7h16M9 7V4h6v3m3 0-1 14H7L6 7m4 4v6m4-6v6"/>', info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v5m0-8h.01"/>', close:'<path d="m6 6 12 12M18 6 6 18"/>', plug:'<path d="m12 22 3-3-2-2 5-5-6-6-5 5-2-2-3 3 10 10Z"/><path d="m10 8 3 3"/>', file:'<path d="M5 3h9l5 5v13H5V3Z"/><path d="M14 3v5h5M8 13h8m-8 4h8"/>'
};
const icon = (name, label='') => `<svg class="icon" viewBox="0 0 24 24" aria-hidden="${!label}">${label ? `<title>${label}</title>` : ''}${icons[name] || icons.info}</svg>`;
document.querySelectorAll('[data-icon]').forEach(el => el.innerHTML = icon(el.dataset.icon));

const state = { user:null, csrf:null, items:[], categories:[], facilities:[], route:'dashboard', draft:[] };
const $ = (s, root=document) => root.querySelector(s);
const $$ = (s, root=document) => [...root.querySelectorAll(s)];
const fmt = new Intl.NumberFormat('th-TH');
const dateFmt = new Intl.DateTimeFormat('th-TH', { day:'numeric', month:'short', year:'2-digit' });
const routeTitles = { dashboard:'ภาพรวมคลัง', province:'ภาพรวมสต็อกทั้งจังหวัด', mapping:'AI Mapping Studio', inbound:'รับเข้าคลัง', outbound:'เบิกจ่ายออกจากคลัง', stock:'รายการคงคลัง', data:'ข้อมูลและการเชื่อมต่อ' };

async function api(url, options={}) {
  const opts = { ...options, headers:{ 'Content-Type':'application/json', ...(options.headers || {}) } };
  if (state.csrf && opts.method && opts.method !== 'GET') opts.headers['X-CSRF-Token'] = state.csrf;
  const res = await fetch(url, opts);
  const content = res.headers.get('content-type') || '';
  const data = content.includes('json') ? await res.json() : await res.text();
  if (!res.ok) {
    if (res.status === 401 && url !== '/api/login') showLogin();
    const err = new Error(data?.error?.message || 'ไม่สามารถดำเนินการได้'); err.data=data; err.status=res.status; throw err;
  }
  return data;
}
function esc(value='') { return String(value).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])); }
function fdate(value) { return value ? dateFmt.format(new Date(`${value}T00:00:00`)) : 'ไม่ระบุ'; }
function today() { return new Date().toISOString().slice(0,10); }
function toast(message, type='success') { const el=document.createElement('div'); el.className=`toast ${type}`; el.innerHTML=`<span>${icon(type==='success'?'check':'alert')}</span><strong>${esc(message)}</strong>`; $('#toast-region').append(el); setTimeout(()=>el.remove(),3600); }
function statusLabel(status){ return ({normal:'พร้อมใช้',low:'ต่ำกว่าจุดเตือน',expiring:'ใกล้หมดอายุ',expired:'หมดอายุ',ready:'พร้อมใช้',design:'รอเชื่อมต่อ',pending:'พร้อมตรวจ',review:'ต้องทบทวน',approved:'อนุมัติแล้ว',rejected:'ปฏิเสธแล้ว'})[status] || status; }
function setLoading(){ $('#main-content').innerHTML=`<div class="grid kpi-grid">${'<div class="skeleton"></div>'.repeat(4)}</div><div class="skeleton skeleton-large"></div>`; }
function pageHead(iconName, tone, title, subtitle, actions=''){ return `<div class="page-head"><div class="page-title-wrap"><div class="page-icon ${tone}">${icon(iconName)}</div><div class="page-title"><h1>${title}</h1><p>${subtitle}</p></div></div><div class="page-actions">${actions}</div></div>`; }
function itemName(id){ return state.items.find(i=>i.id===id)?.name || id; }

async function init() {
  try { const s=await api('/api/session'); state.user=s.user; state.csrf=s.csrf; showApp(); }
  catch { showLogin(); }
}
function showLogin(){ state.user=null; $('#app-view').hidden=true; $('#login-view').hidden=false; setTimeout(()=>$('#username')?.focus(),50); }
function showApp(){ $('#login-view').hidden=true; $('#app-view').hidden=false; $('#profile-name').textContent=state.user?.name || 'ผู้ดูแลคลัง'; route(); }

$('#login-form').addEventListener('submit', async e=>{
  e.preventDefault(); const btn=e.submitter; const err=$('#login-error'); err.textContent=''; btn.disabled=true; btn.lastElementChild.textContent='กำลังเข้าสู่ระบบ…';
  try { const data=await api('/api/login',{method:'POST',body:JSON.stringify({username:$('#username').value.trim(),password:$('#password').value})}); state.user=data.user; state.csrf=data.csrf; showApp(); }
  catch(ex){ err.textContent=ex.message; $('#password').focus(); }
  finally{ btn.disabled=false; btn.lastElementChild.textContent='เข้าสู่ระบบ'; }
});
$('#toggle-password').addEventListener('click',()=>{ const p=$('#password'); p.type=p.type==='password'?'text':'password'; });
$('#logout-button').addEventListener('click',async()=>{ try{await api('/api/logout',{method:'POST',body:'{}'});}finally{showLogin();} });
$('#alerts-button').addEventListener('click',()=>{ location.hash='stock?status=expiring'; });
window.addEventListener('hashchange',route);

async function loadItems(){ const data=await api('/api/items'); state.items=data.items; state.categories=data.categories; }
async function route(){
  if (!state.user) return;
  const raw=(location.hash.slice(1)||'dashboard'); const [name,query='']=raw.split('?'); state.route=['dashboard','province','mapping','inbound','outbound','stock','data'].includes(name)?name:'dashboard';
  $$('[data-route]').forEach(a=>a.classList.toggle('active',a.dataset.route===state.route)); document.title=`${routeTitles[state.route]} | Stock Logistics Sisaket`; setLoading();
  try { if (!state.items.length) await loadItems(); if(state.route==='dashboard')await dashboard(); if(state.route==='province')await provincePage(); if(state.route==='mapping')await mappingPage(); if(state.route==='inbound')await transactionPage('inbound'); if(state.route==='outbound')await transactionPage('outbound'); if(state.route==='stock')await stockPage(new URLSearchParams(query)); if(state.route==='data')await dataPage(); $('#main-content').focus({preventScroll:true}); }
  catch(ex){ $('#main-content').innerHTML=`<div class="card"><div class="notice warning">${icon('alert')}<div><strong>โหลดข้อมูลไม่สำเร็จ</strong><br>${esc(ex.message)}</div></div><button class="button secondary" onclick="location.reload()">ลองใหม่</button></div>`; }
}

async function dashboard(){
  const d=await api('/api/dashboard'); $('#alert-count').textContent=d.kpis.expiring+d.kpis.low;
  const max=Math.max(1,...d.activity.flatMap(x=>[x.inbound,x.outbound]));
  $('#main-content').innerHTML=`<section class="dashboard-hero reveal">
      <div class="dashboard-hero-copy"><div class="hero-badge">${icon('activity')} SISAKET SMART STOCK <span>LIVE OPERATIONS</span></div><h1>ศูนย์บัญชาการ<br><em>คลังยาอัจฉริยะ</em></h1><p>เห็นสถานะเวชภัณฑ์ การเคลื่อนไหว และความเสี่ยงสำคัญจาก <strong>${esc(d.meta.warehouse)}</strong> ในมุมมองเดียว พร้อมขยายการเชื่อมต่อทั้งจังหวัด</p><div class="hero-actions"><a class="button smart-primary" href="#inbound">${icon('plus')} บันทึกรับเข้า</a><a class="button glass" href="/api/export/inventory.csv">${icon('download')} ส่งออกข้อมูล</a></div><div class="hero-meta"><span><b class="live-dot"></b> ระบบคลังพร้อมใช้งาน</span><span>${icon('shield')} ตรวจสอบย้อนหลังได้</span><span>${icon('database')} อ้างอิงข้อมูล Excel</span></div></div>
      <div class="stock-visual" aria-label="ภาพรวมสุขภาพคลัง"><div class="stock-orbit orbit-a"></div><div class="stock-orbit orbit-b"></div><div class="stock-core">${icon('boxes')}<strong>${fmt.format(d.kpis.itemLines)}</strong><small>รายการคงคลัง</small></div><span class="stock-node n-a">AI</span><span class="stock-node n-b">22</span><span class="stock-node n-c">API</span><div class="visual-readout"><b>${fmt.format(d.kpis.low+d.kpis.expiring)}</b><span>รายการต้องดูแล</span></div></div>
    </section>
    ${pageHead('dashboard','', 'ภาพรวมการปฏิบัติงาน', `${esc(d.meta.warehouse)} · อัปเดตจากข้อมูล Excel ต้นฉบับ`, `<a class="button secondary" href="#province">${icon('network')} ดูทั้งจังหวัด</a><a class="button primary" href="#mapping">${icon('sparkles')} AI Mapping</a>`)}
    <div class="grid kpi-grid">
      ${kpi('package','', 'รายการคงคลัง',fmt.format(d.kpis.itemLines),'รายการที่ใช้งาน')}${kpi('alert','orange','ต่ำกว่าจุดเตือน',fmt.format(d.kpis.low),'ควรพิจารณาจัดหา')}${kpi('clock','red','ใกล้หมดอายุ',fmt.format(d.kpis.expiring),'ภายใน 180 วัน')}${kpi('hospital','blue','หน่วยงานในระบบ',fmt.format(d.kpis.facilities),'ปลายทางเบิกจ่าย')}
    </div>
    ${d.alerts.length?`<section class="alert-banner"><div class="card-head"><div class="alert-title"><span>${icon('alert')}</span><div><h2>รายการที่ต้องจัดการ</h2><p>เรียงตามความเร่งด่วนจากวันหมดอายุและระดับคงคลัง</p></div></div><a class="text-link" href="#stock?status=expiring">ดูทั้งหมด ${icon('chevron')}</a></div><div class="alert-list">${d.alerts.slice(0,3).map(alertCard).join('')}</div></section>`:''}
    <div class="grid content-grid"><section class="card"><div class="card-head"><div><h2>แนวโน้มการเคลื่อนไหว 6 เดือน</h2><p>จำนวนรวมเชิงปริมาณจากรายการรับเข้าและเบิกจ่าย (หน่วยอาจต่างกัน)</p></div><div class="chart-legend"><span>รับเข้า</span><span>เบิกจ่าย</span></div></div><div class="chart">${d.activity.map(x=>`<div class="chart-group"><div class="bar inbound h-${Math.round(x.inbound/max*10)}" title="รับเข้า ${fmt.format(x.inbound)}"></div><div class="bar outbound h-${Math.round(x.outbound/max*10)}" title="เบิกจ่าย ${fmt.format(x.outbound)}"></div><label>${x.month.slice(5)}/${x.month.slice(2,4)}</label></div>`).join('')}</div></section>
    <section class="card"><div class="card-head"><div><h2>กิจกรรมล่าสุด</h2><p>ประวัติการเคลื่อนไหวที่บันทึกแล้ว</p></div></div><div class="timeline">${d.recent.map(timelineRow).join('')||'<div class="empty-inline">ยังไม่มีกิจกรรม</div>'}</div></section></div>`;
}
function kpi(iconName,tone,label,value,note){return `<article class="kpi-card ${tone||'green'}"><div class="kpi-icon ${tone}">${icon(iconName)}</div><div class="kpi-meta"><small>${label}</small><strong>${value}</strong><div><em>${note}</em></div></div><span class="kpi-arc" aria-hidden="true"></span></article>`}
function alertCard(i){return `<div class="alert-item"><div><strong title="${esc(i.name)}">${esc(i.name)}</strong><small>${i.status==='low'?`คงเหลือ ${fmt.format(i.qty)} ${esc(i.unit)}`:`หมดอายุ ${fdate(i.expiry)}`}</small></div><span class="status ${i.status}">${statusLabel(i.status)}</span></div>`}
function timelineRow(t){return `<div class="timeline-row"><div class="timeline-icon ${t.type}">${icon(t.type==='inbound'?'inbound':'outbound')}</div><div><strong>${t.type==='inbound'?'รับเข้าจาก':'เบิกจ่ายให้'} ${esc(t.facility)}</strong><small>${esc(t.refNo)} · ${t.items.length} รายการ</small></div><time>${fdate(t.date)}</time></div>`}

function pctClass(value){ return `pct-${Math.max(0,Math.min(100,Math.round(Number(value||0)/5)*5))}`; }
function syncLabel(status){ return ({online:'เชื่อมต่อแล้ว',delayed:'ข้อมูลล่าช้า',offline:'ออฟไลน์'})[status] || status; }
function riskClass(score){ return score < 70 ? 'critical' : score < 85 ? 'watch' : 'healthy'; }

async function provincePage(){
  const d=await api('/api/province/overview');
  $('#alert-count').textContent=d.summary.openMappings+d.summary.offline;
  $('#main-content').innerHTML=`
    <section class="smart-hero reveal"><div class="hero-glow glow-a"></div><div class="hero-glow glow-b"></div><div class="hero-copy"><div class="hero-badge">${icon('wifi')} PROVINCIAL COMMAND CENTER <span>DEMO NETWORK</span></div><h1>มองเห็นสต็อกทั้งจังหวัด<br><em>ก่อนที่ของจะขาด</em></h1><p>รวมสัญญาณจาก ${fmt.format(d.summary.hospitals)} โรงพยาบาล ตรวจความพร้อมแบบข้ามคลัง และค้นหาโอกาสโยกสต็อกในหน้าจอเดียว</p><div class="hero-actions"><a class="button smart-primary" href="#mapping">${icon('sparkles')} เปิด AI Mapping</a><button class="button glass" id="refresh-network">${icon('wifi')} ซิงก์สถานะใหม่</button></div><div class="hero-meta"><span><b class="live-dot"></b> ${fmt.format(d.summary.online)} แห่งออนไลน์</span><span>อัปเดต ${new Date(d.generatedAt).toLocaleTimeString('th-TH',{hour:'2-digit',minute:'2-digit'})} น.</span><span>ข้อมูลเครือข่ายจำลองเพื่อออกแบบระบบ</span></div></div>
      <div class="network-orb" aria-label="สถานะเครือข่ายโรงพยาบาล"><div class="orb-ring ring-one"></div><div class="orb-ring ring-two"></div><div class="orb-core">${icon('network')}<strong>${d.summary.hospitals}</strong><small>โรงพยาบาล</small></div>${d.hospitals.slice(0,12).map((h,i)=>`<span class="orb-node node-${i+1} ${h.syncStatus}" title="${esc(h.name)}"></span>`).join('')}</div>
    </section>
    <div class="grid smart-kpis reveal delay-1">${smartKpi('hospital','blue','โรงพยาบาลในเครือข่าย',d.summary.hospitals,`${d.summary.online} online · ${d.summary.delayed} delayed`)}${smartKpi('target','green','ความพร้อมใช้งาน',`${d.summary.readiness}%`,'ค่าเฉลี่ยทั้งจังหวัด')}${smartKpi('alert','orange','รายการต่ำกว่าจุดเตือน',d.summary.lowStock,'รวมทุกโรงพยาบาล')}${smartKpi('sparkles','purple','รอ AI Mapping',d.summary.openMappings,'ต้องยืนยันโดยมนุษย์')}</div>
    <div class="grid province-panels reveal delay-2"><section class="card network-card"><div class="card-head"><div><h2>เครือข่ายการซิงก์</h2><p>สถานะแต่ละโรงพยาบาลตามรอบเชื่อมต่อ</p></div><div class="sync-legend"><span class="online">ออนไลน์</span><span class="delayed">ล่าช้า</span><span class="offline">ออฟไลน์</span></div></div><div class="network-matrix">${d.hospitals.map(h=>`<div class="network-node ${h.syncStatus}" title="${esc(h.name)}"><span>${esc(h.district.slice(0,2))}</span><b></b><small>${esc(h.district)}</small></div>`).join('')}</div><div class="system-strip">${d.systemCounts.map(s=>`<span>${icon('database')} ${esc(s.name)} <b>${s.count}</b></span>`).join('')}</div></section>
      <section class="card transfer-panel"><div class="card-head"><div><h2>Smart Rebalancing</h2><p>ข้อเสนอโยกสต็อกข้ามโรงพยาบาล</p></div><span class="ai-chip">${icon('sparkles')} AI INSIGHT</span></div><div class="transfer-list">${d.rebalancing.map(transferCard).join('')}</div><button class="button secondary wide" type="button" id="view-transfer-note">ตรวจรายละเอียดก่อนดำเนินการ</button></section></div>
    <section class="card table-card reveal delay-3"><div class="table-tools"><div class="table-heading"><h2>สุขภาพสต็อกทุกโรงพยาบาล</h2><p>เรียงจากหน่วยงานที่ต้องดูแลก่อน</p></div><label class="search">${icon('search')}<input id="hospital-search" placeholder="ค้นหาโรงพยาบาลหรืออำเภอ" aria-label="ค้นหาโรงพยาบาล"></label><select id="sync-filter" class="filter-select" aria-label="กรองสถานะซิงก์"><option value="all">ทุกสถานะ</option><option value="online">ออนไลน์</option><option value="delayed">ข้อมูลล่าช้า</option><option value="offline">ออฟไลน์</option></select></div><div id="hospital-table"></div></section>`;
  const update=()=>renderHospitalTable(d.hospitals.filter(h=>{const q=$('#hospital-search').value.trim().toLowerCase(),s=$('#sync-filter').value;return(!q||`${h.name} ${h.district}`.toLowerCase().includes(q))&&(s==='all'||h.syncStatus===s)}));
  $('#hospital-search').addEventListener('input',update); $('#sync-filter').addEventListener('change',update); update();
  $('#refresh-network').addEventListener('click',async e=>{e.currentTarget.disabled=true;e.currentTarget.innerHTML=`${icon('wifi')} กำลังซิงก์…`;await new Promise(r=>setTimeout(r,650));toast('อัปเดตสถานะเครือข่ายแล้ว');await provincePage();});
  $('#view-transfer-note').addEventListener('click',()=>toast('ข้อเสนอทุกใบต้องผ่านผู้อนุมัติก่อนสร้างใบโอนจริง'));
}
function smartKpi(iconName,tone,label,value,note){return `<article class="smart-kpi"><div class="smart-kpi-icon ${tone}">${icon(iconName)}</div><div><small>${label}</small><strong>${typeof value==='number'?fmt.format(value):esc(value)}</strong><p>${note}</p></div><span class="kpi-spark ${tone}"></span></article>`}
function transferCard(row){return `<article class="transfer-card ${row.urgency}"><div class="transfer-item"><span>${icon('package')}</span><div><strong>${esc(row.item?.name||row.itemId)}</strong><small>${esc(row.reason)}</small></div><b>${fmt.format(row.qty)} ${esc(row.unit)}</b></div><div class="transfer-route"><span>${esc(row.from?.district||'-')}</span>${icon('arrowRight')}<span>${esc(row.to?.district||'-')}</span></div></article>`}
function renderHospitalTable(rows){$('#hospital-table').innerHTML=`<div class="table-wrap"><table class="hospital-table"><thead><tr><th>โรงพยาบาล</th><th>ระบบเชื่อมต่อ</th><th>ซิงก์ล่าสุด</th><th>ความพร้อม</th><th class="number">รายการคลัง</th><th class="number">ต่ำกว่าจุดเตือน</th><th class="number">ยังไม่แมป</th><th>Stock health</th></tr></thead><tbody>${rows.length?rows.map(h=>`<tr><td><div class="hospital-cell"><span class="hospital-logo ${riskClass(h.stockScore)}">${esc(h.district.slice(0,1))}</span><div><strong>${esc(h.name)}</strong><small>${esc(h.level)} · อ.${esc(h.district)}</small></div></div></td><td><span class="system-badge">${esc(h.system)}</span></td><td><span class="sync-state ${h.syncStatus}"><b></b>${syncLabel(h.syncStatus)}</span><small>${fmt.format(h.lastSyncMinutes)} นาทีที่แล้ว</small></td><td><div class="mini-progress"><span class="${pctClass(h.readiness)}"></span></div><small>${h.readiness}% ready</small></td><td class="number">${fmt.format(h.itemLines)}</td><td class="number"><strong class="risk-number">${fmt.format(h.lowStock)}</strong></td><td class="number">${fmt.format(h.unmapped)}</td><td><span class="health-score ${riskClass(h.stockScore)}">${h.stockScore}</span></td></tr>`).join(''):`<tr><td colspan="8" class="table-empty">ไม่พบโรงพยาบาลที่ตรงกับตัวกรอง</td></tr>`}</tbody></table></div>`}

async function mappingPage(status='open'){
  const d=await api(`/api/ai/mappings?status=${status}`); const open=d.counts.pending+d.counts.review;
  $('#main-content').innerHTML=`
    <section class="ai-hero reveal"><div class="ai-copy"><div class="hero-badge">${icon('sparkles')} SMART ITEM INTELLIGENCE</div><h1>AI Mapping Studio</h1><p>ทำความเข้าใจชื่อยา รหัส และหน่วยที่ต่างกันจากแต่ละโรงพยาบาล แล้วเสนอรหัสกลางพร้อมเหตุผลที่ตรวจสอบได้</p><div class="ai-guard">${icon('shield')} AI เสนอ · เภสัชกรตัดสินใจ · ระบบบันทึก Audit ทุกครั้ง</div></div><div class="brain-stage" aria-hidden="true"><div class="brain-halo halo-one"></div><div class="brain-halo halo-two"></div><div class="brain-core">${icon('brain')}</div><i class="data-particle p1"></i><i class="data-particle p2"></i><i class="data-particle p3"></i></div><div class="ai-stats"><span><b>${open}</b>รอตรวจ</span><span><b>${d.counts.approved}</b>อนุมัติแล้ว</span><span><b>100%</b>Human control</span></div></section>
    <div class="grid mapping-layout reveal delay-1"><section class="mapping-queue"><div class="mapping-toolbar"><div><h2>คิวรอตรวจสอบ</h2><p>${fmt.format(d.mappings.length)} รายการ · เรียงตามความมั่นใจ</p></div><select id="mapping-filter" class="filter-select"><option value="open">งานที่เปิดอยู่</option><option value="all">ทั้งหมด</option><option value="pending">พร้อมตรวจ</option><option value="review">ต้องทบทวน</option><option value="approved">อนุมัติแล้ว</option><option value="rejected">ปฏิเสธแล้ว</option></select></div><div id="mapping-list" class="mapping-list">${d.mappings.length?d.mappings.map(mappingCard).join(''):`<div class="card empty-ai">${icon('sparkles')}<h3>ไม่มีงานในคิวนี้</h3><p>ลองเลือกสถานะอื่น หรือทดสอบรายการใหม่ด้านขวา</p></div>`}</div></section>
      <aside class="mapping-aside"><form id="ai-suggest-form" class="card ai-lab"><div class="lab-head"><span>${icon('brain')}</span><div><h2>ทดลอง AI Mapping</h2><p>วิเคราะห์แบบไม่บันทึกลง master</p></div></div><label class="field compact"><span>โรงพยาบาล <b class="required">*</b></span><select class="control" name="hospitalId" required><option value="">— เลือกโรงพยาบาล —</option>${d.hospitals.map(h=>`<option value="${h.id}">${esc(h.name)}</option>`).join('')}</select></label><label class="field compact"><span>รหัสจากระบบต้นทาง</span><input class="control" name="sourceCode" placeholder="เช่น PCM500-GPO"></label><label class="field compact"><span>ชื่อจากระบบต้นทาง <b class="required">*</b></span><input class="control" name="sourceName" required minlength="3" placeholder="เช่น PARA 500 GPO 50x10"></label><label class="field compact"><span>หน่วย <b class="required">*</b></span><input class="control" name="unit" required placeholder="กล่อง / ขวด / แผง"></label><button class="button ai-button wide" type="submit">${icon('sparkles')} วิเคราะห์ด้วย AI</button><p class="engine-note"><b></b> Engine: ${esc(d.engine.mode)}<br>ผลลัพธ์ต้องได้รับการยืนยันก่อนใช้งาน</p></form><div id="ai-suggest-results" class="suggest-results"></div></aside></div>`;
  $('#mapping-filter').value=status; $('#mapping-filter').addEventListener('change',e=>mappingPage(e.target.value));
  $$('.mapping-decision').forEach(btn=>btn.addEventListener('click',()=>decideMapping(btn.dataset.id,btn.dataset.decision)));
  $('#ai-suggest-form').addEventListener('submit',submitMappingSuggest);
}
function mappingCard(m){const isOpen=['pending','review'].includes(m.status);return `<article class="mapping-card"><div class="mapping-top"><div class="source-system"><span>${icon('database')}</span><div><small>${esc(m.hospital?.name||m.hospitalId)} · ${esc(m.sourceSystem)}</small><strong>${esc(m.sourceName)}</strong><code>${esc(m.sourceCode)} · ${esc(m.unit)}</code></div></div><div class="confidence ${m.confidence>=.95?'high':m.confidence>=.9?'good':'review'}"><b>${Math.round(m.confidence*100)}%</b><span>confidence</span></div></div><div class="mapping-flow"><div class="flow-label">AI SUGGESTION</div><div class="mapped-item"><span class="ai-mark">${icon('sparkles')}</span><div><small>รายการกลางที่แนะนำ</small><strong>${esc(m.suggestedItem?.name||'-')}</strong><code>${esc(m.suggestedItem?.code||'')} · ${esc(m.suggestedItem?.unit||'')}</code></div><span class="status ${m.status}">${statusLabel(m.status)}</span></div><div class="confidence-bar"><span class="${pctClass(m.confidence*100)}"></span></div><ul class="reason-list">${m.reasons.map(r=>`<li>${icon('check')}${esc(r)}</li>`).join('')}</ul></div>${isOpen?`<div class="mapping-actions"><label class="field compact mapping-choice"><span>ยืนยันรายการกลาง</span><select class="control mapping-select" data-id="${m.id}">${state.items.map(i=>`<option value="${i.id}" ${i.id===m.suggestedItemId?'selected':''}>${esc(i.code)} · ${esc(i.name)}</option>`).join('')}</select></label><button class="button secondary mapping-decision" data-id="${m.id}" data-decision="reject">ส่งกลับ</button><button class="button ai-button mapping-decision" data-id="${m.id}" data-decision="approve">${icon('check')} อนุมัติ Mapping</button></div>`:`<div class="mapping-reviewed">${icon('shield')} ตรวจสอบแล้ว ${m.reviewedAt?new Date(m.reviewedAt).toLocaleString('th-TH',{dateStyle:'medium',timeStyle:'short'}):''}</div>`}</article>`}
async function decideMapping(id,decision){const button=$(`.mapping-decision[data-id="${id}"][data-decision="${decision}"]`);if(button)button.disabled=true;try{const itemId=$(`.mapping-select[data-id="${id}"]`)?.value;await api(`/api/ai/mappings/${id}/decision`,{method:'POST',body:JSON.stringify({decision,itemId})});toast(decision==='approve'?'อนุมัติ Mapping และบันทึก Audit แล้ว':'ส่งรายการกลับเพื่อตรวจสอบแล้ว');await mappingPage($('#mapping-filter')?.value||'open')}catch(ex){toast(ex.message,'error');if(button)button.disabled=false}}
async function submitMappingSuggest(e){e.preventDefault();const btn=e.submitter;btn.disabled=true;btn.innerHTML=`${icon('sparkles')} กำลังวิเคราะห์…`;const input=Object.fromEntries(new FormData(e.currentTarget));try{const d=await api('/api/ai/mappings/suggest',{method:'POST',body:JSON.stringify(input)});$('#ai-suggest-results').innerHTML=`<div class="suggest-head"><span>${icon('sparkles')}</span><div><strong>พบ ${d.suggestions.length} รายการใกล้เคียง</strong><small>${esc(d.source.sourceName)}</small></div></div>${d.suggestions.map((s,i)=>`<article class="suggest-card ${i===0?'best':''}"><div><small>${i===0?'BEST MATCH':`ALTERNATIVE ${i}`}</small><strong>${esc(s.item.name)}</strong><code>${esc(s.item.code)} · ${esc(s.item.unit)}</code></div><b>${Math.round(s.confidence*100)}%</b><div class="confidence-bar"><span class="${pctClass(s.confidence*100)}"></span></div><p>${esc(s.reasons.join(' · '))}</p></article>`).join('')}`;}catch(ex){toast(ex.message,'error')}finally{btn.disabled=false;btn.innerHTML=`${icon('sparkles')} วิเคราะห์ด้วย AI`}}

async function transactionPage(type){
  state.draft=[]; const inbound=type==='inbound'; const d=await api(`/api/transactions?type=${type}`); state.facilities=d.facilities;
  $('#main-content').innerHTML=`${pageHead(inbound?'inbound':'outbound',inbound?'':'orange',inbound?'รับเข้าคลัง':'เบิกจ่ายออกจากคลัง',inbound?'บันทึกแหล่งที่มา ล็อต และวันหมดอายุให้ตรวจสอบย้อนหลังได้':'ตัดสต็อกแบบตรวจจำนวนคงเหลือก่อนยืนยัน',`<button class="button secondary" id="clear-form">ล้างแบบฟอร์ม</button>`)}
    <div class="notice ${inbound?'':'warning'}">${icon(inbound?'info':'alert')}<div><strong>${inbound?'หลักการรับเข้า':'ระบบป้องกันสต็อกติดลบ'}</strong><br>${inbound?'ข้อมูลล็อตและวันหมดอายุจะอัปเดตบนรายการคงคลัง':'หากจำนวนไม่พอ ระบบจะยกเลิกทั้งเอกสารและไม่ตัดยอดบางส่วน'}</div></div>
    <form id="transaction-form" class="card form-card" novalidate>
      <div class="form-section"><div class="section-title ${inbound?'':'orange'}"><span>${icon('file')}</span><h2>ข้อมูลเอกสาร</h2></div><div class="form-grid"><label class="field compact"><span>${inbound?'ผู้ส่งมอบ / แหล่งที่มา':'หน่วยงานผู้เบิก'} <b class="required">*</b></span><input class="control" name="facility" list="facilities-list" required placeholder="ระบุหรือเลือกหน่วยงาน"><datalist id="facilities-list">${d.facilities.map(x=>`<option value="${esc(x)}">`).join('')}</datalist></label><label class="field compact"><span>วันที่ทำรายการ <b class="required">*</b></span><input class="control" type="date" name="date" value="${today()}" required></label><label class="field compact"><span>เลขที่อ้างอิง</span><input class="control" name="refNo" placeholder="สร้างอัตโนมัติหากเว้นว่าง"></label><label class="field compact"><span>หมายเหตุ</span><input class="control" name="note" placeholder="รายละเอียดเพิ่มเติม"></label></div></div>
      <div class="form-section"><div class="section-title ${inbound?'':'orange'}"><span>${icon('package')}</span><h2>เพิ่มรายการ${inbound?'รับเข้า':'เบิกจ่าย'}</h2></div>
        <div class="line-entry"><label class="field compact item-select"><span>รายการ <b class="required">*</b></span><select class="control" id="line-item"><option value="">— เลือกรายการ —</option>${state.items.map(i=>`<option value="${i.id}">${esc(i.name)} · คงเหลือ ${fmt.format(i.qty)} ${esc(i.unit)}</option>`).join('')}</select></label><label class="field compact"><span>จำนวน <b class="required">*</b></span><input class="control" id="line-qty" type="number" min="0.01" step="0.01" placeholder="0"></label><label class="field compact"><span>ล็อต</span><input class="control" id="line-lot" placeholder="Lot no."></label><label class="field compact"><span>วันหมดอายุ</span><input class="control" id="line-expiry" type="date"></label><button class="button ${inbound?'primary':'orange'} add-line" type="button" id="add-line">${icon('plus')} เพิ่ม</button></div>
        <div id="draft-lines" class="draft-table"></div>
      </div><div class="form-footer"><button class="button secondary" type="button" id="cancel-draft">ยกเลิก</button><button class="button ${inbound?'primary':'orange'}" type="submit">${icon('check')} ยืนยัน${inbound?'รับเข้า':'เบิกจ่าย'}</button></div>
    </form>
    <section class="card"><div class="card-head"><div><h2>สรุปรายการ${inbound?'รับเข้า':'เบิกจ่าย'}ล่าสุด</h2><p>${fmt.format(d.transactions.length)} เอกสารที่บันทึกแล้ว</p></div></div><div class="transaction-list">${d.transactions.slice(0,6).map(t=>transactionCard(t,type)).join('')||'<div class="empty-inline">ยังไม่มีรายการ</div>'}</div></section>`;
  renderDraft(type); $('#add-line').addEventListener('click',()=>addDraft(type)); $('#line-item').addEventListener('change',syncLineDefaults); $('#transaction-form').addEventListener('submit',e=>submitTransaction(e,type)); $('#clear-form').addEventListener('click',()=>clearTransactionForm(type)); $('#cancel-draft').addEventListener('click',()=>{state.draft=[];renderDraft(type)});
}
function syncLineDefaults(){const i=state.items.find(x=>x.id===$('#line-item').value); if(!i)return; $('#line-lot').value=i.lot||''; $('#line-expiry').value=i.expiry||'';}
function addDraft(type){ const item=state.items.find(i=>i.id===$('#line-item').value); const qty=Number($('#line-qty').value); if(!item||!qty||qty<=0)return toast('เลือกรายการและระบุจำนวนให้ถูกต้อง','error'); if(type==='outbound'&&qty>item.qty)return toast(`คงเหลือไม่พอ: ${fmt.format(item.qty)} ${item.unit}`,'error'); const exists=state.draft.find(x=>x.itemId===item.id); if(exists)exists.qty+=qty; else state.draft.push({itemId:item.id,qty,lot:$('#line-lot').value.trim(),expiry:$('#line-expiry').value||null}); renderDraft(type); $('#line-item').value='';$('#line-qty').value='';$('#line-lot').value='';$('#line-expiry').value='';$('#line-item').focus();}
function renderDraft(type){ const root=$('#draft-lines'); if(!root)return; if(!state.draft.length){root.innerHTML=`<div class="empty-inline">${icon('package')}ยังไม่มีรายการ กรอกข้อมูลด้านบนแล้วกด “เพิ่ม”</div>`;return;} root.innerHTML=`<div class="table-wrap"><table><thead><tr><th>#</th><th>รายการ</th><th>ล็อต</th><th>วันหมดอายุ</th><th class="number">จำนวน</th><th></th></tr></thead><tbody>${state.draft.map((r,n)=>{const i=state.items.find(x=>x.id===r.itemId);return `<tr><td>${n+1}</td><td><strong>${esc(i.name)}</strong><small>${esc(i.code)} · คงเหลือ ${fmt.format(i.qty)} ${esc(i.unit)}</small></td><td>${esc(r.lot||'-')}</td><td>${fdate(r.expiry)}</td><td class="number"><span class="stock-number">${fmt.format(r.qty)}</span> ${esc(i.unit)}</td><td><button class="icon-button remove-line" type="button" data-index="${n}" aria-label="ลบรายการ">${icon('trash')}</button></td></tr>`}).join('')}</tbody></table></div>`; $$('.remove-line',root).forEach(b=>b.addEventListener('click',()=>{state.draft.splice(Number(b.dataset.index),1);renderDraft(type)}));}
async function submitTransaction(e,type){e.preventDefault();if(!state.draft.length)return toast('เพิ่มอย่างน้อย 1 รายการก่อนยืนยัน','error');const fd=new FormData(e.currentTarget);const btn=e.submitter;btn.disabled=true;try{await api('/api/transactions',{method:'POST',body:JSON.stringify({type,facility:fd.get('facility'),date:fd.get('date'),refNo:fd.get('refNo'),note:fd.get('note'),items:state.draft})});toast(type==='inbound'?'บันทึกรับเข้าเรียบร้อย':'บันทึกเบิกจ่ายเรียบร้อย');state.items=[];await loadItems();await transactionPage(type);}catch(ex){toast(ex.message,'error')}finally{btn.disabled=false}}
function clearTransactionForm(type){state.draft=[];$('#transaction-form').reset();$('#transaction-form [name=date]').value=today();renderDraft(type)}
function transactionCard(t,type){return `<article class="transaction-card"><div class="top"><strong>${esc(t.refNo)}</strong><span class="status normal">บันทึกแล้ว</span></div><p>${esc(t.facility)}</p><div class="bottom"><span>${fmt.format(t.lineCount)} รายการ · ${fmt.format(t.totalQty)} หน่วยรวม</span><time>${fdate(t.date)}</time></div></article>`}

async function stockPage(params=new URLSearchParams()){
  const preset=params.get('status')||'all'; const data=await api('/api/items'); state.items=data.items;state.categories=data.categories;
  $('#main-content').innerHTML=`${pageHead('boxes','purple','รายการคงคลัง','ค้นหา ติดตามจุดสั่งซื้อ และตรวจวันหมดอายุจากรายการปัจจุบัน',`<a class="button secondary" href="/api/export/inventory.csv">${icon('download')} ส่งออก CSV</a><a class="button primary" href="#inbound">${icon('plus')} รับเข้า</a>`)}
    <div class="grid summary-grid">${kpi('package','','พร้อมใช้งาน',fmt.format(data.items.filter(i=>i.status==='normal').length),'รายการ')}${kpi('alert','orange','ต่ำกว่าจุดเตือน',fmt.format(data.items.filter(i=>i.status==='low').length),'รายการ')}${kpi('clock','red','หมดอายุใน 180 วัน',fmt.format(data.items.filter(i=>['expiring','expired'].includes(i.status)).length),'รายการ')}</div>
    <section class="card table-card"><div class="table-tools"><label class="search">${icon('search')}<input id="stock-search" placeholder="ค้นหาชื่อ รหัส หรือเลขล็อต" aria-label="ค้นหารายการ"></label><select id="status-filter" class="filter-select" aria-label="กรองสถานะ"><option value="all">ทุกสถานะ</option><option value="normal">พร้อมใช้</option><option value="low">ต่ำกว่าจุดเตือน</option><option value="expiring">ใกล้หมดอายุ</option><option value="expired">หมดอายุ</option></select><select id="category-filter" class="filter-select" aria-label="กรองหมวดหมู่"><option value="all">ทุกหมวดหมู่</option>${data.categories.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('')}</select><span class="source-badge">แหล่งข้อมูล: คงคลังปัจจุบัน</span></div><div id="stock-table"></div></section>`;
  $('#status-filter').value=preset; const update=()=>renderStockTable(data.items.filter(i=>{const q=$('#stock-search').value.trim().toLowerCase(),s=$('#status-filter').value,c=$('#category-filter').value;return(!q||`${i.name} ${i.code} ${i.lot}`.toLowerCase().includes(q))&&(s==='all'||i.status===s)&&(c==='all'||i.category===c)})); $('#stock-search').addEventListener('input',update);$('#status-filter').addEventListener('change',update);$('#category-filter').addEventListener('change',update);update();
}
function renderStockTable(items){$('#stock-table').innerHTML=`<div class="table-wrap stock-table-wrap"><table class="stock-table"><thead><tr><th>รายการ</th><th>หมวดหมู่</th><th>ล็อต / ตำแหน่ง</th><th>วันหมดอายุ</th><th class="number">จุดเตือน</th><th class="number">คงเหลือ</th><th>สถานะ</th></tr></thead><tbody>${items.length?items.map(i=>`<tr><td data-label="รายการ"><div class="item-cell"><span class="item-avatar">${esc(i.code.slice(0,3))}</span><div><strong>${esc(i.name)}</strong><small>${esc(i.code)} · ${esc(i.package||'ไม่ระบุบรรจุภัณฑ์')}</small></div></div></td><td data-label="หมวดหมู่">${esc(i.category)}</td><td data-label="ล็อต / ที่เก็บ"><strong>${esc(i.lot||'-')}</strong><small>${esc(i.location||'ไม่ระบุ')}</small></td><td data-label="วันหมดอายุ"><strong>${fdate(i.expiry)}</strong><small>${Number.isFinite(i.daysToExpiry)?`${fmt.format(i.daysToExpiry)} วัน`:'ไม่มีวันหมดอายุ'}</small></td><td data-label="จุดเตือน" class="number">${fmt.format(i.minQty)}</td><td data-label="คงเหลือ" class="number"><span class="stock-number">${fmt.format(i.qty)}</span> ${esc(i.unit)}</td><td data-label="สถานะ"><span class="status ${i.status}">${statusLabel(i.status)}</span></td></tr>`).join(''):`<tr><td colspan="7" class="table-empty">${icon('search')}<br>ไม่พบรายการที่ตรงกับตัวกรอง</td></tr>`}</tbody></table></div>`}

async function dataPage(){const d=await api('/api/master-data');
  $('#main-content').innerHTML=`${pageHead('database','','ข้อมูลและการเชื่อมต่อ','จัดการรายการกลาง พร้อมจุดเชื่อมต่อสำหรับระบบโรงพยาบาล',`<a class="button secondary" href="/api/v1/stock" target="_blank">${icon('plug')} ดู REST API</a><button class="button primary" id="new-item">${icon('plus')} เพิ่มรายการกลาง</button>`)}
    <div class="notice">${icon('info')}<div><strong>รองรับด้วย Integration Layer</strong><br>การเชื่อม HIS/HOSxP/ERP จริงต้องทำ data mapping และทดสอบกับ interface ของแต่ละโรงพยาบาลก่อน ไม่ควรเชื่อมฐานข้อมูลผลิตจริงโดยตรง</div></div>
    <div class="grid split-grid"><section class="card"><div class="card-head"><div><h2>ช่องทางเชื่อมต่อ</h2><p>มาตรฐานกลางสำหรับแลกเปลี่ยนข้อมูล</p></div></div><div class="integration-grid">${d.integrations.map(x=>`<article class="integration-card"><div class="integration-logo">${icon(x.id==='int-csv'?'file':'plug')}</div><div><h3>${esc(x.name)}</h3><p>${esc(x.kind)} · ${esc(x.detail)}</p><span class="status ${x.status}">${statusLabel(x.status)}</span></div></article>`).join('')}</div></section>
    <section class="card"><div class="card-head"><div><h2>ข้อมูลระบบ</h2><p>แหล่งที่มาและขอบเขตต้นแบบ</p></div></div><div class="meta-list"><div class="meta-row"><span>หน่วยงาน</span><strong>${esc(d.meta.organization)}</strong></div><div class="meta-row"><span>คลัง</span><strong>${esc(d.meta.warehouse)}</strong></div><div class="meta-row"><span>ไฟล์ต้นทาง</span><strong>${esc(d.meta.sourceWorkbook)}</strong></div><div class="meta-row"><span>ชีตหลัก</span><strong>${esc(d.meta.sourceSheet)}</strong></div><div class="meta-row"><span>รายการที่นำเข้า</span><strong>${fmt.format(state.items.length)} รายการ</strong></div></div></section></div>
    <section class="card spaced-card"><div class="card-head"><div><h2>บันทึกตรวจสอบย้อนหลัง</h2><p>กิจกรรมสำคัญในระบบจะถูกเก็บแบบ append-only ในต้นแบบ</p></div></div><div class="audit-list">${d.audit.map(a=>`<div class="audit-row"><span>${icon('activity')}</span><div><strong>${esc(a.detail)}</strong><small>${esc(a.action)} · ${esc(a.user)}</small></div><time>${new Date(a.at).toLocaleString('th-TH',{dateStyle:'medium',timeStyle:'short'})}</time></div>`).join('')}</div></section>`;
  $('#new-item').addEventListener('click',openItemModal);
}
function openItemModal(){
  $('#modal-root').innerHTML=`<div class="modal-backdrop" role="presentation"><form class="modal" id="item-form" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div class="modal-head"><h2 id="modal-title">เพิ่มรายการกลาง</h2><button class="icon-button" type="button" data-close aria-label="ปิด">${icon('close')}</button></div><div class="modal-body"><div class="notice warning">${icon('info')}<div>รายการใหม่จะเริ่มต้นที่ยอด 0 กรุณาใช้เมนูรับเข้าเพื่อเพิ่มจำนวนและล็อต</div></div><div class="form-grid two"><label class="field compact"><span>รหัสรายการ <b class="required">*</b></span><input class="control" name="code" required placeholder="เช่น PCM-GPO-500"></label><label class="field compact"><span>ชื่อรายการ <b class="required">*</b></span><input class="control" name="name" required></label><label class="field compact"><span>หมวดหมู่ <b class="required">*</b></span><input class="control" name="category" list="category-list" required><datalist id="category-list">${state.categories.map(x=>`<option value="${esc(x)}">`).join('')}</datalist></label><label class="field compact"><span>หน่วย <b class="required">*</b></span><input class="control" name="unit" required placeholder="กล่อง / ขวด / ชุด"></label><label class="field compact"><span>รูปแบบบรรจุ</span><input class="control" name="package"></label><label class="field compact"><span>จุดเตือนคงคลัง</span><input class="control" name="minQty" type="number" min="0" value="0"></label><label class="field compact"><span>ตำแหน่งจัดเก็บ</span><input class="control" name="location" placeholder="A-01"></label></div><div id="item-error" class="form-error modal-error"></div></div><div class="modal-foot"><button class="button secondary" type="button" data-close>ยกเลิก</button><button class="button primary" type="submit">${icon('check')} บันทึกรายการ</button></div></form></div>`;
  $$('[data-close]',$('#modal-root')).forEach(b=>b.addEventListener('click',closeModal)); $('.modal-backdrop').addEventListener('click',e=>{if(e.target===e.currentTarget)closeModal()}); $('#item-form').addEventListener('submit',submitItem); $('#item-form [name=code]').focus();
}
function closeModal(){ $('#modal-root').innerHTML=''; }
async function submitItem(e){e.preventDefault();const btn=e.submitter;btn.disabled=true;const obj=Object.fromEntries(new FormData(e.currentTarget));try{await api('/api/items',{method:'POST',body:JSON.stringify(obj)});toast('เพิ่มรายการกลางเรียบร้อย');closeModal();state.items=[];await loadItems();await dataPage();}catch(ex){$('#item-error').textContent=ex.message}finally{btn.disabled=false}}

init();
